[build-system]
requires = ["hatchling", "uv-dynamic-versioning"]
build-backend = "hatchling.build"


[project]
name = "ratatosk"
version = "0.1.0"
authors = [
  { name = "Nik Reeves-McLaren", email = "nik.reeves-mclaren@sheffield.ac.uk"},
  { name = "Neil Shephard", email = "n.shephard@sheffield.ac.uk" },
]
description = "Validated electrochemical data-processing, analysis and integrity tool for battery research."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.14"
classifiers = [
  "Development Status :: 1 - Planning",
  "Intended Audience :: Science/Research",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering",
  "Typing :: Typed",
]
dependencies = [
  "lmfit>=1.3.4",
  "matplotlib>=3.11.2",
  "numpy>=2.5.3",
  "scipy>=1.18.1",
]

[project.urls]
Homepage = "https://github.com/RSE-Sheffield/ratatosk"
"Bug Tracker" = "https://github.com/RSE-Sheffield/ratatosk/issues"
Discussions = "https://github.com/RSE-Sheffield/ratatosk/discussions"
Changelog = "https://github.com/RSE-Sheffield/ratatosk/releases"


[dependency-groups]
docs = [
  "zensical>=0.0.63",
]
tests = [
  "pytest >=9",
  "pytest-cov >=7",
  "pytest-loguru>=0.4.1",
  "pytest-mpl>=0.19.0",
  "pytest-testmon>=2.2.0",
  "pytest-xdist>=3.8.0",
  "syrupy>=6.1.1",
]
notebooks = [
  "jupyter>=1.1.1",
  "marimo>=0.24.2",
]
dev = [
  { include-group = "docs" },
  { include-group = "notebooks" },
  { include-group = "tests" },
  "black>=26.5.1",
  "codespell>=2.4.3",
  "ipdb>=0.13.13",
  "mypy>=2.3.1",
  "pre-commit>=4.6.2",
  "pylint>=4.0.8",
  "ruff>=0.16.8",
  "scipy-stubs>=1.18.1.1",
  "ty>=0.0.82",
]


[tool.pytest]
minversion = "9.0"
addopts = ["-ra", "--showlocals --cov"]
strict = true
filterwarnings = [
  "error",
]
log_level = "INFO"
testpaths = [
  "tests",
]


[tool.codespell]
skip = 'uv.lock'
count = ''
quiet-level = 3
# ignore-words-list = ''

[tool.coverage]
run.source = ["ratatosk"]
report.exclude_also = [
  '\.\.\.',
  'if typing.TYPE_CHECKING:',
]

[tool.mypy]
files = ["src", "tests"]
python_version = "3.14"
warn_unused_configs = true
strict = true
enable_error_code = ["ignore-without-code", "redundant-expr", "truthy-bool"]
warn_unreachable = true
disallow_untyped_defs = false
disallow_incomplete_defs = false

[[tool.mypy.overrides]]
module = "ratatosk.*"
disallow_untyped_defs = true
disallow_incomplete_defs = true


[tool.ruff]
show-fixes = true

[tool.ruff.lint]
extend-select = [
  "ARG",    # flake8-unused-arguments
  "B",      # flake8-bugbear
  "C4",     # flake8-comprehensions
  "EM",     # flake8-errmsg
  "EXE",    # flake8-executable
  "FURB",   # refurb
  "G",      # flake8-logging-format
  "ICN",    # flake8-import-conventions
  "ISC",    # flake8-implicit-str-concat
  "LOG",    # flake8-logging
  "NPY",    # NumPy specific rules
  "PD",     # pandas-vet
  "PERF",   # perflint
  "PGH",    # pygrep-hooks
  "PL",     # pylint
  "PT",     # flake8-pytest-style
  "PTH",    # flake8-use-pathlib
  "PYI",    # flake8-pyi
  "Q",      # flake8-quotes
  "RET",    # flake8-return
  "RSE",    # flake8-raise
  "RUF",    # Ruff-specific
  "SIM",    # flake8-simplify
  "SLOT",   # flake8-slots
  "T10",    # flake8-debugger
  "T20",    # flake8-print
  "TC",     # flake8-type-checking
  "TRY",    # tryceratops
  "UP",     # pyupgrade
]
ignore = [
  "PLR09",    # Too many <...>
  "PLR2004",  # Magic value used in comparison
]
# Uncomment if using a _compat.typing backport
# typing-modules = ["ratatosk._compat.typing"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["T20"]
"noxfile.py" = ["T20"]
"docs/examples/**" = ["T20"]


[tool.pylint]
py-version = "3.14"
ignore-paths = [".*/_version.py"]
reports.output-format = "colorized"
similarities.ignore-imports = "yes"
messages_control.disable = [
  "design",
  "fixme",
  "line-too-long",
  "missing-module-docstring",
  "missing-function-docstring",
  "wrong-import-position",
]

[tool.ty.rules]
possibly-unresolved-reference = "warn"

[tool.hatch.version]
source = "uv-dynamic-versioning"
